<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sand"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_pxportrait_title"
            style="@style/Theme.Reap.Title"
            android:text="@string/pixel_portraits" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_pxportrait_portraits"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </LinearLayout>

    <include
        android:id="@+id/ol_pxportrait_single"
        layout="@layout/overlay_pixelportrait_single"
        android:visibility="invisible" />
</FrameLayout>