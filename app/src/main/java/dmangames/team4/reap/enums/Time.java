package dmangames.team4.reap.enums;

import static java.util.concurrent.TimeUnit.MINUTES;

/**
 * Created by stevenzhang on 4/24/16.
 */
public class Time {
    public static final long POMODORO_WORK_SECS = MINUTES.toSeconds(1);
    public static final long POMODORO_BREAK_SECS = MINUTES.toSeconds(1);
    public static final long COUNT_UP_SECS = MINUTES.toSeconds(1);
}
